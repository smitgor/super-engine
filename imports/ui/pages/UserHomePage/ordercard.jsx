import React, { useEffect, useState } from 'react';


const OrderCard = (orderDetails) => {

  return (
    <div className='order-card' key={ele.id}>

      {/* <img src={ele.img} alt="" />
      <div>{ele.desc}</div>
      <div>{ele.price} x {ele.quantity}</div>
      <div>{ele.price*ele.quantity}</div> */}
    </div>
  )
}

export default OrderCard